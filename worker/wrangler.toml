name = "zizai-sync"
main = "src/index.ts"
compatibility_date = "2026-01-01"
workers_dev = true

# R2 bucket 绑定（对象版本控制须在 bucket 上开启，见 README）
[[r2_buckets]]
binding = "ZIZAI_BUCKET"
bucket_name = "zizai"

# SYNC_TOKEN 是 secret：不要写在这里，用 `wrangler secret put SYNC_TOKEN` 设置。
