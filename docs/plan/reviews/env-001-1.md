# Review env-001-1 — 环境与脚手架

- 任务: [docs/plan/tasks/env-001.md](../tasks/env-001.md)
- 上下文: [docs/plan/README.md](../README.md) M1、[docs/plan/analysis/modules.md](../analysis/modules.md) §2
- 评审时间: 2026-08-06
- 评审人: verify agent（只读评审，未改动代码）

## 结论: **pass**

Objective 4 项全部达成，与设计文档 contract 一致，无残留 stub/mock（默认 counter 模板属脚手架产物，见非阻塞发现）。

## 逐项证据

### 1. Flutter SDK 安装

```text
$ ~/flutter/bin/flutter --version
Flutter 3.44.8 • channel stable • https://github.com/flutter/flutter.git
Framework • revision 058e0af2c2 (2026-07-23)
Tools • Dart 3.12.2 • DevTools 2.57.0
```

版本与上下文声明（3.44.8 stable，Intel mac）一致。PATH 已配置于 `~/.zshenv`（`export PATH="$HOME/flutter/bin:$PATH"` 后命令可直接运行）。

### 2. flutter create 三平台 + 命名 contract

三平台目录均存在：

```text
$ ls windows macos android
android/  → app, build.gradle.kts, gradle, gradle.properties, gradlew, local.properties, settings.gradle.kts
macos/    → Flutter, Runner, Runner.xcodeproj, Runner.xcworkspace, RunnerTests
windows/  → CMakeLists.txt, flutter, runner
```

org `dev.zizai` / 项目名 `zi_zai` 落地检查：

- `pubspec.yaml`: `name: zi_zai`
- `android/app/build.gradle.kts`: `namespace = "dev.zizai.zi_zai"`、`applicationId = "dev.zizai.zi_zai"`
- `macos/Runner/Configs/AppInfo.xcconfig`: `PRODUCT_BUNDLE_IDENTIFIER = dev.zizai.ziZai`（underscore→camelCase，flutter create 标准转换，RunnerTests 为 `dev.zizai.ziZai.RunnerTests`）
- `windows/runner/Runner.rc`: `ProductName = zi_zai`、`OriginalFilename = zi_zai.exe`

### 3. 8 个依赖齐全且与 modules.md §2 一致

`pubspec.yaml` dependencies 中第三方包（env-001 引入的 6 个 + 模块文档另要求的 2 个）：

| 包 | 版本 | modules.md §2 |
|---|---|---|
| sqflite | ^2.4.3 | ✓ |
| sqflite_common_ffi | ^2.4.2 | ✓ |
| path_provider | ^2.1.6 | ✓ |
| flutter_quill | ^11.5.1 | ✓ |
| file_selector | ^1.1.0 | ✓ |
| share_plus | ^13.3.0 | ✓ |
| http | ^1.6.0 | ✓ |
| crypto | ^3.0.7 | ✓ |

`pubspec.lock` 已生成（27400 bytes），`flutter pub get` 成功（analyze/test 可运行即证明依赖解析完整）。

### 4. git 基线提交

```text
$ git log --oneline
5ccfc55 chore: Flutter 脚手架基线（zi_zai v0.1.0，三平台）
971f2f2 docs: 产品设计文档与交付计划
337c59f Initial commit

$ git log -1 --format='%an <%ae>'
zhangmo8 <wegi866@gmail.com>
```

基线提交存在、作者为 zhangmo8、工作树干净（`git status --short` 为空），`build/` 未入库（`git ls-files` 中 0 个 build 文件）。数据库 `zi-zai.db` 位于运行时应用支持目录（path_provider），不落仓库，符合 Objective 4。

### 5. 验证命令

```text
$ flutter analyze
Analyzing zizai...  No issues found! (ran in 10.0s)

$ flutter test
00:00 +0: loading test/widget_test.dart
00:00 +0: Counter increments smoke test
00:01 +1: All tests passed!
```

M1 出口标准（`flutter create` 三平台目录存在，`flutter test` 通过）满足。

## 非阻塞发现与建议（建议记入 backlog.md）

1. **默认 counter 模板保留**：`lib/main.dart` 与 `test/widget_test.dart` 仍为 flutter create 生成的计数器 demo，非产品功能 stub/mock，不违反 env-001 contract（该任务仅要求脚手架 + 依赖 + 基线提交）。建议在 store-001/shell-001 开始时替换为真实实现，并同步删除/改写 counter 测试。
2. **任务文件路径过时**：`docs/plan/tasks/env-001.md` 的 `Path` 字段写的是 `/Users/ssa-user/dim-agent`，与实际项目根 `/Users/ssa-user/Desktop/study/github/zizai` 不一致，属文档笔误，建议修正。
3. **shell 环境噪音**：每次 shell 启动报 `no such file or directory: /Users/ssa-user/.cargo/env`（`~/.zshenv` 引用了不存在的 cargo 环境），不影响 Flutter 工具链，属环境卫生问题，可后续清理。
