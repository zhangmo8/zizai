# Review set-004-1 — 设置页与导出

- 评审对象：commit `d352d46`（`lib/ui/settings_view.dart` 新增；`lib/ui/editor.dart` 设置按钮 + 对话框/全屏入口；`lib/ui/status_bar.dart` 今日进度点击；`lib/core/models.dart` Settings.copyWith；`lib/state/settings_controller.dart` dbPath；`test/ui/settings_test.dart` 8 用例）+ `39a54af`（Ctrl/Cmd+S 全局 handler，与 set-004 目标无关的修复，已确认不越界）
- 依据：docs/plan/tasks/set-004.md（Objective 4 项 + Verification）；docs/app/ui-settings.md（Region Layout / Interactions / State Variants / Component Tree / Page Entry Points）；docs/app/style.md（§5 对话框 480px/内边距 24、§6 圆角 8/6、§9 组件样式）；docs/app/README.md §4（settings 键）/§8（平台差异导出）
- 范围分界：同步区归 sync-ui-003、关于区归 upd-001——本任务未实现（符合）
- 前置评审：docs/plan/reviews/edit-003-1.md（pass）
- 评审方式：只读代码评审 + 全量命令验证（工作树除无关的 `output/` 未跟踪目录外干净，d352d46/39a54af 均在 main 历史，直接评审当前检出态；未修改任何代码文件）
- 日期：2026-08-06

## 结论：pass

Objective 4 项全部达成；无残留 stub/mock（导出为真实默认实现 + 测试注入点）；1 条实现取舍经确认可接受（字体为预设候选列表而非系统字体枚举，行为契约满足，见偏差①）。`flutter analyze` 无错误；`flutter test` 69/69 通过（4 次全量中 1 次既有 editor「崩溃恢复」flake，隔离复跑通过，对应 backlog 25）。非阻塞发现 5 条建议追加 backlog。

## 验证命令结果（当前检出态 d352d46 + 39a54af）

| 命令 | 结果 |
|---|---|
| `flutter analyze` | `No issues found!`（2.5s） |
| `flutter test` | 4 次全量：3 次 `+69: All tests passed!`；1 次 `+68 -1`（editor_test「崩溃恢复：启动出现确认条 → 恢复 → 内容入库」flake）——该用例隔离重跑通过、editor_test 整文件连续 3/3 通过、前后两次全量均 69/69。属既有时序 flake（backlog 25 同类别，非本提交引入） |

## 逐项证据

### Objective 1 — 外观区：主题 / 字体 / 字号 / 行距，即改即存实时生效 + 示例预览 ✅（偏差①见下）

- 主题三态下拉：`跟随系统/浅色/深色` → `_update(_s.copyWith(theme: v))`（settings_view.dart:203-216）；`SettingsController.themeMode` 映射三态（settings_controller.dart:21-25），`app.dart` 以 ListenableBuilder 监听 settings 接线 `themeMode`（app.dart:122-130）——改即生效。
- 即改即存：`update()` 先 `notifyListeners()` 再 `_db.saveSettings`（settings_controller.dart:41-46）；`Settings.fromMap` 对 theme 白名单 + 数值钳制（models.dart:163-186），持久化非法值有兜底。
- 字体下拉：含「系统默认」（空串）+ 7 款常见中文字体（settings_view.dart:23-32）；选择即存并作用于编辑器（`_buildStyles` fontFamily，editor.dart:345-351）。**偏差①**：预设候选列表而非「系统可用字体枚举」——Flutter 无跨端系统字体枚举官方 API，预设列表 + 未安装字体回退系统渲染为合理取舍，行为契约（下拉可选 + 即改即存 + 编辑器生效）满足，确认接受（同 edit-003 偏差①先例），列入非阻塞 1。
- 字号 Slider 12–28 步进 1（divisions=16）、行距 Slider 1.2–2.4 步进 0.1（divisions=12），拖动即 `_update`（settings_view.dart:140-149, 232-258）；编辑器实时应用：Shell `Listenable.merge` 监听 settings（shell.dart:105）→ EditorView 重建 → `customStyles: _buildStyles(s)` 带新字号/行距（editor.dart:307-341）。
- 示例预览：`_preview()` 以同款 fontSize/height/fontFamily 渲染「预览：字在 天地玄黄 AaBb 123」（settings_view.dart:261-279），hairline 边框 + 6px 圆角（style.md §6 按钮 6px 一致）。
- 测试：主题切换即改即存并持久化（settings_test「主题切换」）、字号 Slider → fontSize 更新 + 预览字号同步（settings_test「字号 Slider」）✅。

### Objective 2 — 写作区：每日目标 100–50000 + 状态栏即时刷新 ✅

- 数字输入框（keyboardType number，hint `100–50000`），`onSubmitted` 校验 `int.tryParse` 且 100 ≤ n ≤ 50000 才写入（settings_view.dart:281-303）——非法输入不生效；`Settings.fromMap` 持久化侧同样钳制 100–50000（models.dart:170-184）。
- 状态栏即时刷新：StatusBar 读 `settings.settings.dailyGoal` 计算进度（status_bar.dart:109-111），Shell ListenableBuilder 监听 settings 触发重建。
- 入口：状态栏今日进度 `InkWell` → `onOpenSettings(true)`（status_bar.dart:126-135）→ `_openSettings(focusGoal: true)` → `SettingsView(autoFocusDailyGoal: true)` → 打开后 `requestFocus` 定位目标输入框（settings_view.dart:69-74；editor.dart:370-390）——ui-settings.md Page Entry Points 第 2 条 ✅。
- 测试：3000 → 关闭后状态栏「今日 0/3000」（settings_test「每日目标输入」）；非法「50」不生效（settings_test「非法目标输入」）✅。

### Objective 3 — 数据区：db 路径 + 打开目录 + 导出（平台差异 + 无文档禁用）✅

- db 路径展示：`SettingsController.dbPath ← Db.path`（settings_controller.dart:31；db.dart:122），只读展示、ellipsis（settings_view.dart:305-322）。
- 「打开目录」：macOS `open` / Windows `explorer` / Linux `xdg-open` 打开 db 父目录（settings_view.dart:113-122）；Android 隐藏（`if (!isAndroidPlatform)`，ui-settings.md Region Layout「Android 隐藏」一致）。
- 导出：注入点 `ExportHandler? exporter`（settings_view.dart:34-35, 49-50），null 时默认实现——Android `SharePlus.instance.share` 分享纯文本 / 桌面 `getSaveLocation(suggestedName: '$title.txt')` + 写入（settings_view.dart:103-111），README §8「桌面系统保存对话框 / Android 系统分享」✅；导出内容经 `exportPlainText`（Delta → 纯文本，export.dart:63）。
- 当前无文档：整行替换为说明「先打开一个文档」（settings_view.dart:324-332）——State Variants「无文档：导出按钮禁用 + 说明」，实现以说明文本替代禁用按钮，功能契约（不可导出 + 附说明）满足，视觉解释差异可接受（列非阻塞 5）。测试断言说明文本出现、无「导出」按钮 ✅。
- 导出失败：catch → 数据区下方 danger 色错误条「导出失败: $e」（settings_view.dart:85-101, 159-169）——State Variants「导出失败：错误提示条」✅。
- 测试：注入 exporter 回调收到当前文档与纯文本「正文」（settings_test「导出：有文档可用」）✅。

### Objective 4 — 恢复默认不删数据；Esc/× 关闭；入口 ✅

- 恢复默认：`_update(const Settings())` 仅重置设置（settings_view.dart:171-174），不触碰任何文档；测试断言主题回 system 且 notebooks/documents 仍各 1（settings_test「恢复默认」）✅。
- × 关闭：`_Header` 关闭 IconButton → `Navigator.maybePop`（settings_view.dart:132, 349-371）；测试经「关闭」tooltip 关闭 ✅。
- Esc 关闭：桌面 `showDialog` → `DialogRoute` 默认 `barrierDismissible: true`，Flutter 框架层 Esc 触发 `maybePop`（flutter routes.dart:1717-1718 明确「pressing the escape key … will pop」）；Shell 全局 Esc handler 在非沉浸态返回 false 不消费事件（shell.dart:90-98），对话框 Esc 关闭不被遮蔽 ✅。Android 为 `MaterialPageRoute(fullscreenDialog)`，系统返回键关闭（平台差异合理）。
- 入口：顶栏设置按钮（editor.dart:438-442，测试「顶栏设置按钮 → 对话框出现」✅）；桌面 `showDialog` 480px 宽（editor.dart:383-389，style.md §5）内边距 24（settings_view.dart:135）；Android 全屏页（`fullscreenDialog`，ui-settings.md「桌面对话框 / Android 全屏页」）。Drawer 底部入口在 README 层面为 Android 入口，实现经顶栏（Android 同样有顶栏设置按钮），Drawer 入口未单列——ui-settings.md Page Entry Points 第 3 条为设计项，顶栏按钮在 Android 端可用，功能可达，列非阻塞 4。
- 范围分界：外观/写作/数据三区齐全（settings_view.dart:137-158），无同步区、无关于区 ✅。

### Verification（widget test）✅

`test/ui/settings_test.dart` 8 条：入口与三区渲染、主题即改即存持久化、字号 Slider + 预览同步、目标字数 → 状态栏刷新、非法输入不生效、导出回调收到当前文档、无文档禁用并说明、恢复默认不删文档。测试缺口见非阻塞 3。

## 已知偏差确认

| # | 偏差 | 评审判定 |
|---|---|---|
| ① | 字体下拉为预设候选列表（PingFang SC/Heiti SC/Songti SC/Kaiti SC/Noto Sans CJK/Microsoft YaHei/SimSun + 系统默认），非「系统可用字体枚举」 | **确认接受**：Flutter 无跨端系统字体枚举官方 API（代码注释已说明，settings_view.dart:21-22）；未安装字体回退系统渲染，选择即存 + 编辑器生效的行为契约满足。精确枚举列入非阻塞 1 |

## 非阻塞发现（建议追加 backlog）

1. **字体列表为预设候选而非系统字体枚举**（偏差①延续）：跨端不可枚举系统字体；后续可用平台侧 API（如 font_list 插件）或保持候选 + 文档明确。
2. **对话框圆角/阴影未按 style.md §6 定制**：M3 默认 Dialog 圆角 28px（flutter dialog.dart `_DialogDefaultsM3` circular(28)），style.md §6 要求 8px、阴影「1 级极浅」；当前未显式 shape/elevation。纯视觉打磨（同 backlog 15/16/22 类别，建议 M5 统一处理）。
3. **测试缺口**：行距 Slider 对预览/编辑器样式的断言缺失（仅字号有）；状态栏点击 → 聚焦目标输入框无测试；导出失败错误条路径无测试；Esc 关闭对话框无专门测试（依赖框架默认行为，已文档核实）。
4. **Android Drawer 设置入口未实现**：ui-settings.md Page Entry Points 第 3 条（Android Drawer 底部入口）；Android 现经顶栏设置按钮可达，入口位置为设计项，可待移动端打磨补齐或文档调整。
5. **无文档时导出行为**：实现以说明文本整体替代按钮，State Variants 原文为「按钮禁用 + 说明」；功能契约满足，若需严格一致可改为禁用态按钮 + 说明。

## 范围说明

- 本评审未修改任何代码/任务状态文件，仅新增本报告（符合任务约束）；未触碰 backlog.md，非阻塞发现已在报告内列明，由计划循环侧追加。
- 工作树仅有无关注未跟踪目录 `output/`；评审全程未触碰。
- `39a54af`（Ctrl/Cmd+S 全局 handler）已核实为 set-004 之外的独立修复（editor.dart/shell.dart 各 ~10 行），顺带解决 backlog 20（Windows 下 quill 内置 Ctrl+S=codeBlock 遮蔽），与本任务无冲突。

## 建议后续动作

- pass → merge d352d46（含 39a54af）到 main → 全量测试 → set-004 置 done。
- 非阻塞 2（对话框样式）随 M5 打磨；3 中导出失败/聚焦入口测试可随后续设置相关任务补；4 待文档裁决或 Android 打磨。
