# Task pkg-006 — 打包发布

```yaml
id: pkg-006
scope: 全项目
status: pending
depends-on: [integ-005]
```

## Objective

1. macOS：`flutter build macos` 产出 `.app`，签名/公证暂不要求（自用）；zip 打包用于更新分发。
2. Android：`flutter build apk --release` 产出 APK。
3. Windows：本机无 Windows 构建环境，产出构建说明文档（`docs/plan/build-windows.md`：需要 Windows 机器运行 `flutter build windows`，含 VS 工具链要求）。
4. **分发上传**：三端安装包上传 R2 `zizai/apps/`，生成 `update.json`（含 sha256、minDbSchema），更新检查即指向该清单（见 docs/app/update.md §3）。

## Context

- docs/plan/README.md（里程碑 M5）
- docs/app/update.md（§3 分发结构）

## Path

- `build/`（构建产物，gitignore）
- `docs/plan/build-windows.md`
- 上传产物与 `update.json`（R2 bucket，不入库）

## Verification

- macOS `.app` 可启动并正常读写库目录。
- APK 安装到 Android 设备可运行。
- Windows 构建说明文档存在。
- R2 中 `update.json` 可被 `core/update` 拉到并校验通过。
