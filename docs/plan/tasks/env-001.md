# Task env-001 — 环境与脚手架

```yaml
id: env-001
scope: 项目根
status: done
depends-on: []
```

## Objective

1. 安装 Flutter SDK（需用户确认后执行；此前用户取消过一次 brew 安装）。
2. `flutter create` 生成 App（org `dev.zizai`，项目名 `zi_zai`），启用 `windows` / `macos` / `android`。
3. 添加依赖：`sqflite`、`sqflite_common_ffi`、`path_provider`、`flutter_quill`、`file_selector`、`share_plus`。
4. `git init` 并提交基线（`zi-zai.db` 在应用支持目录，不入库）。

## Context

- docs/plan/README.md（里程碑 M1）
- docs/plan/analysis/modules.md（§2 依赖清单）

## Path

- 项目根 `/Users/ssa-user/Desktop/study/github/zizai`
- `lib/`, `test/`, `windows/`, `macos/`, `android/`, `pubspec.yaml`, `.gitignore`

## Verification

- `flutter pub get` 成功；`flutter analyze` 无错误；`flutter test` 通过。
- 三平台目录存在；`git log` 有基线提交。
