# Task set-004 — 设置页与导出

```yaml
id: set-004
scope: lib/ui
status: done
depends-on: [shell-001]
```

## Objective

按 docs/app/ui-settings.md 实现设置：

1. 外观区：主题（跟随系统/浅色/深色）、字体（系统字体列表 + 默认）、字号 Slider 12–28、行距 Slider 1.2–2.4，即改即存并实时作用于编辑器（含示例文本预览）。
2. 写作区：每日目标字数输入（100–50000），状态栏进度即时刷新。
3. 数据区：db 路径展示 + 桌面「打开目录」；导出当前文档——桌面 `file_selector` 保存 .txt，Android `share_plus` 系统分享；当前无文档时禁用。
4. 恢复默认（不删数据）；`Esc`/`×` 关闭。

## Context

- docs/app/ui-settings.md（整体）
- docs/app/README.md（§8 平台差异）
- docs/plan/analysis/requirements.md（F5、F1.6）

## Path

- `lib/ui/settings_view.dart`
- `test/ui/settings_test.dart`

## Verification

- widget test：改主题即生效且持久化；字号/行距作用编辑器；目标字数更新状态栏；导出禁用态；恢复默认。
- `flutter analyze` 无错误；`flutter test` 通过。
